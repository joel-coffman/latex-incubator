\documentclass[tikz]{standalone}

\usepackage{libertinus}
\usepackage{pgfplots}


% pgfplots
\usepgfplotslibrary{categorical}


\begin{document}
\begin{tikzpicture}
  \begin{axis}[
      likert={{ responses | length }},
      enlarge y limits=false,
      width=\linewidth - 35pt,
      y=\baselineskip,
      ytick=data,
      yticklabels={,,},
  ]
    % offset
    \addplot[offset] coordinates {(1.0 - {{ offset }},0.0)};

    % plots
    {% for response, count in responses.items() -%}
    \addplot coordinates {({{ count / respondents }},0) [{{ count }}]};  % {{ response }}
    {% endfor %}

    % legend
    \legend{{ '{{' }}{{ '},{'.join(responses) }}{{ '}}' }};
  \end{axis}
\end{tikzpicture}
\end{document}

